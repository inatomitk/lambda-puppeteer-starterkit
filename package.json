{
  "name": "lambda-puppeteer-starterkit",
  "version": "1.0.0",
  "description": "puppeteer starter kit using AWS Lambda.",
  "author": "inatomitk",
  "license": "MIT",
  "scripts": {
    "test": "NODE_PATH='./layer/nodejs/node_modules' jest",
    "postinstall": "npm --prefix ./layer/nodejs install ./layer/nodejs"
  },
  "dependencies": {
    "config": "^3.0.1",
    "puppeteer-core": "^2.1.1"
  },
  "devDependencies": {
    "jest": "^25.1.0",
    "puppeteer": "^2.1.1",
    "serverless": "^1.38.0"
  }
}
