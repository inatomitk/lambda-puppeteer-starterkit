{
  "name": "lambda-puppeteer-template",
  "version": "1.0.0",
  "description": "puppeteer template using AWS Lambda.",
  "author": "inatomitk",
  "license": "MIT",
  "scripts": {
    "test": "jest",
    "postinstall": "npm --prefix ./layer/nodejs install ./layer/nodejs"
  },
  "dependencies": {
    "config": "^3.0.1",
    "puppeteer-core": "^1.12.2"
  },
  "devDependencies": {
    "jest": "^23.6.0",
    "serverless": "^1.38.0"
  }
}
